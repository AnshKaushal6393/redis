# login_otp_with_ttl

TODO: Add details for the `login_otp_with_ttl` example.

## Expected behavior
- Likely stores/verifies a login OTP in Redis with a TTL.

## How to run
- Start the repo services from the root:

```bash
docker-compose up -d
```

- Then install & run this project:

```bash
cd login_otp_with_ttl
npm install
npm run dev
```

